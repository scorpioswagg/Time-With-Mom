"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Heart, Video } from "lucide-react";
export default function HomePage() {
 const router=useRouter(); const [loading,setLoading]=useState(false); const [error,setError]=useState("");
 async function createRoom(){setLoading(true);setError("");try{const res=await fetch("/api/rooms",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:"Time With Mom"})});const data=await res.json();if(!res.ok)throw new Error(data.error||"Could not create room.");router.push(`/room/${data.id}?admin=1`)}catch(e:any){setError(e.message||"Something went wrong.");setLoading(false)}}
 return <main className="min-h-screen grid place-items-center p-6 bg-gradient-to-b from-rose-50 to-white dark:from-zinc-950 dark:to-zinc-900"><div className="w-full max-w-lg text-center space-y-8"><div><div className="mx-auto mb-5 grid place-items-center w-16 h-16 rounded-full bg-rose-100 text-rose-600 dark:bg-rose-900/30"><Heart size={32} fill="currentColor"/></div><h1 className="text-5xl font-bold tracking-tight text-zinc-900 dark:text-white">Time With Mom</h1><p className="mt-3 text-lg text-zinc-600 dark:text-zinc-400">One link. No account. Just time together.</p></div><button onClick={createRoom} disabled={loading} className="w-full py-5 rounded-2xl bg-rose-600 hover:bg-rose-700 text-white text-xl font-semibold shadow-lg disabled:opacity-60 flex items-center justify-center gap-3">{loading?"Creating your room…":<><Video size={25}/> Create Room</>}</button>{error&&<p className="text-sm text-red-600">{error}</p>}<div className="grid sm:grid-cols-2 gap-4 text-left"><div className="p-5 rounded-2xl bg-white/80 dark:bg-zinc-800/50 border border-zinc-200 dark:border-zinc-800"><b>Instant video</b><p className="text-sm text-zinc-500 mt-1">Up to 4 people with high-quality Daily video and audio.</p></div><div className="p-5 rounded-2xl bg-white/80 dark:bg-zinc-800/50 border border-zinc-200 dark:border-zinc-800"><b>Messages when away</b><p className="text-sm text-zinc-500 mt-1">Leave text, voice, or a short video for later.</p></div></div><p className="text-xs text-zinc-400">Chrome · Safari · Firefox · Edge · Mobile friendly</p></div></main>
}